# CLAUDE.md

## 言語

回答・説明・コミットメッセージはすべて日本語で書く。コード内の識別子は英語のままでよい。

## このプロジェクト

「いしひろいマップ」。地図上で地点を選ぶと、その場所の地質から「拾えそうな石」を推定して表示する石拾い向けアプリ。技術検証プロトタイプとして始まっている。

地質データは産総研 地質調査総合センターの「20万分の1日本シームレス地質図V2」Web API を使う。

## 構成

ビルドなしの静的サイト。フレームワーク・パッケージマネージャは使っていない。

- `index.html` / `script.js` — 地図画面。地質取得と石カード表示
  - **置くものの「種類」で場所を決める。同じ形のものを並べない。** 浮かぶ丸い札に、名前（操作しない）・検索（操作する）・選択地点（結果の表示）・地質図の切り替え（操作）を全部のせると、4つが同じ重さに見えて、どれが主役か分からなくなる。次の3つに分ける
    1. **帯（`.app-header`、高さ52px）** … 名前（`.app-brand`）とメニューだけ。動かない枠なので、操作しないものの置き場になる。検索をここに入れてはいけない（検索は地図を操作するためのもの）
    2. **地図** … 操作だけ。検索（`.map-search-bar`）は左上、**ズームと地質図の切り替えは右上に縦1列**。地図の上には結果を表示しない
    3. **結果パネル（`.result-pane`）** … いちばん上が「選択地点」の見出し（`.result-place`）。地図の上に帯で出すと、地質図の切り替えと同じ形になって、表示なのか操作なのか分からなくなる
  - **見出しの地名は「いま出している結果の地点」を指す。選んだだけの地点で書き換えない。** カードは「ここを調べる！」を押すまで入れ替わらないので、選んだ時点で書き換えると、前の地点の結果に別の地名の見出しが付く（実際にそうなっていて、滑川市の見出しの下に白馬の石が並んだ）。`inspectGeology()` が結果を消すときに `hideSelectedPlace()`、`draw()` で `showSelectedPlace()` する。住所は地質より遅れて返ることがあるので、`updateSelectedAddress()` は `isInspectedPoint()` で「いま出している結果の地点の住所か」を確かめてから書き換える
  - **地図の操作は、同じ大きさ・同じ角丸のアイコンでそろえる**（`.map-icon-button` とズームをどちらも36px幅・角丸10px）。文字つきのスイッチと細い＋−のように幅が違うものを重ねると、そろっていないように見える。地質図の切り替えは `aria-pressed` の押しボタンで、出ているあいだは `.is-on`（アクセント色）にする
  - Leaflet は追加した順に積むので、右上の並び順は `.leaflet-top.leaflet-right` を flex にして `order` で決める。上の余白は入れ物の `padding-top` に持たせる（`order` を使うと `:first-of-type` は見た目の1番目と一致しない）
  - **地図と結果は重ねない。** `.app-shell`（高さ `calc(100vh - 52px)`）を `grid-template-columns: minmax(0, 1fr) var(--panel-w)` で左右に分け、結果には角丸も影も付けず、境目は1本の線（`border-left`）にする。地図の上に浮かせると、結果が「閉じられる付箋」に見えて、消す方法があると勘違いさせる
  - 重ねないので、地図の上の操作は地図の面の中だけを見ればよい（`--panel-gap` は地図の内側の余白）。**`--panel-w` を使って右端を逃がす指定は要らない**
  - 折りたたむと `grid-template-columns: 0 minmax(0, 1fr)` になる。つまみ（`.map-collapse-button`）は地図の面の中に収める。境目にまたがらせると、地図の `overflow: hidden` ではみ出した半分が切れる
  - 調べ終わったら `hideInspectPopup()` でふきだしごと消す。地名は結果パネルの見出しに出ているので、住所だけのふきだしを残すと同じ内容が2か所に出る
  - 現在地・検索の失敗は「選択地点」ではないので、見出しではなく `setPanelMessage(rocksPanel, ...)` で結果パネルの案内として出す
  - 切り替えは**半分ずつの2枚のタブ**（`grid-template-columns: 1fr 1fr`）。幅を中身に合わせると、2つの項目の大きさがちぐはぐになり、パネルの中で行き場のない形になる。いま見ているほうは下線（`::after`）で示す。`.result-head` に入れて上に貼り付ける（sticky）
  - タブの下線をパネルの幅いっぱいに引くため、左右の余白は `.result-head` ではなく中身（`.result-place` と `.tab-panel`）に持たせる。`.result-head` の背景は、下線で中身を切るのでぼかさずに塗る
  - 何も選んでいないときは `.panel-intro`（丸い目印＋見出し＋手順3つ）を出す。手順は最初だけで、`setPanelMessage()` / `clearPanelMessage()` が `hidePanelSteps()` を呼んで以後は出さない。`hidden` は `display: grid` に負けるので `.panel-steps[hidden]` で明示的に消している
  - 狭い画面（820px以下）では地図が上、結果が下。ここでも重ねず、境目は1本の線（`border-top`）にする
  - 参考にした地図: 地理院地図（帯＋地図いっぱい）、Electricity Maps（検索は地図の上、操作は同じ大きさのアイコンを縦1列、凡例や日時などの「表示」には面を持たせない）
- `stone.html` / `stone.js` — 石の詳細ページ（`?id=` で切り替え）
- `mineral.html` / `mineral.js` — 鉱物の詳細ページ（`?id=` で切り替え）。石・岩相・用語と同じ2段組みで、右に 読み・特徴 と目次。「入っていることが多い石」は写真のタイル（`stoneTile()`）で出す
- `lithology.html` / `lithology.js` — 地質（岩相）の詳細ページ（`?id=` に symbol 接尾辞）。地図画面「地質を見る」のカードと上流域の一覧からリンクする
  - 石のページと同じ2段組み。右の要点欄に 区分（と地質の分類マップへのリンク）・地質図の記号・時代と地図の色、左の本文に 拾えそうな石 → 見どころの鉱物 → 名前の読み方 → おもな岩石の名前の決まり方
  - 「時代と地図の色」は時代名が長い（最長46字、最多26件）。要点欄の中でこの項目だけ横幅いっぱい（`.facts-wide`）にし、「古生代」などの代は小さく前置きにして、6件目からは `<details>` で畳む
  - 拾えそうな石は `lithology-map.json` の weight を「この地質そのもの／一緒に出ることが多い／混じることがある」に言い換え、写真のタイル（`stoneTile()`）で出す
  - 見どころの鉱物は `estimateMinerals()` と同じ2通り（母岩・変成帯の名前）で、周辺の割り引きなしに計算する
  - 「名前の読み方」は岩相名に出てくる用語（高P/T型、緑泥石帯、海洋 など）の説明。`TERMS` に正規表現で持っている。「海洋」「島弧・大陸」は付加体と火成岩で意味が違うので `SETTING_TERMS` に分けている
  - 「おもな岩石の名前の決まり方」は、火成岩・堆積岩・付加体の地質で「この地質そのもの」の石についてだけ出す。変成岩の地質では出さない。対応表には変成前の岩石（斑れい岩・チャートなど）が入っているので、その物差しを出すと変成後の実物と食い違う
- `term.html` / `term.js` — 用語の説明ページ（`?id=` で切り替え）。本文は `data/terms.json` の `sections` から組み立てる。2段組みで、右に 読み・別名・種類 と目次。`sections[].items` は `catalogRow()` で並べ、石なら写真を添える（一覧に石が1つでもあれば、写真のない項目にも「用語」「鉱物」の枠を出して列をそろえる）
- `glossary.html` / `glossary.js` — 用語集（辞書の索引の形）。地図画面右上のメニューから開く。石・鉱物・用語をまとめて `reading`（読み）の五十音順に行ごとに並べ、上に「あ か さ…」の帯（sticky）を置く
  - **詳細ページのカードの見た目は使わない**。検索欄を主役に、1件1行（種類・名前・読み）の密な並びにして、広い画面では CSS の `columns` で2〜4段に流し込む。1件の中身は最大273pxなので、列を広げるのではなく本数を増やす（幅は920px／1280px以上で1200px）。読みは名前のすぐ右に置く（右端に寄せると行の中が間延びする）。説明文は行に出さず、`link-preview.js` のホバーカードに任せる（そのため用語集も `link-preview.js` を読み、`registerCatalog()` でデータを渡す）
  - 絞り込みの `hidden` は `display: flex` に負けるので、`.glossary-filters[hidden]` で明示的に消している
  - 種類（石／鉱物／地質／用語）で絞り込め、石はさらに `category`（火成岩・堆積岩・変成岩・その他）で絞り込める。「地質」は `terms.json` の `category: "地質"` のもの
  - 検索は名前・読み・別名・英名・説明が対象（ひらがなとカタカナは区別しない）。検索語と絞り込みは `?q=` `kind=` `sub=` に残す
  - 地図の岩相（610件）は名前が長く読みもないので載せていない
- `rock-tree.html` / `rock-tree.js` — 石と鉱物の分類マップ。地図画面右上のメニューから開く。上のバーの切り替えスイッチ（`.tree-switch`）で「石」と「鉱物」を切り替える。どちらを見ているかは `?tab=mineral` に残す。木は同じ枠に描き直し、見方（`[data-tab-guide]`）も入れ替える。木の描き方は `tree-layout.js`（地質の分類マップと共通）
  - **切り替えスイッチは上のバー（戻るボタンの帯）のまん中に、文字だけで置く**（`.tree-header .tree-switch`）。バーはもともと戻るボタンしかなく空いているので、場所を増やさずに、スクロールしてもいつでも切り替えられる。戻るボタンと同じく箱も枠も持たせず、選んでいる側は濃い文字にして下に短い線（`.tree-switch-indicator`）を引く。切り替えると線が滑って移る
    - ここに落ち着くまでに3つ試した。①見出しの下に地図画面と同じ下線のタブ（`.tab-buttons`）→ スクロールで消え、位置も中途半端。②見出しの下に横幅いっぱいのスイッチを貼り付け（アイコン・名前・「でき方で分ける・33種」）→ スクロール中に木を見る場所を狭めて邪魔。③バーの中に灰色の箱＋アイコン＋数のスイッチ → すっきりしたバーの中でそこだけ重たく浮いた
    - 何で分けているか（でき方／成分）は、スイッチではなく下の紹介文（`.rock-tree-sub`）で伝える
    - **ヘッダーに置けるのは移動の部品だけ**（戻るボタンと同じ仲間）。石の木と鉱物の木は分け方も中身も別で、実質は別のページを選ぶ操作なのでここでよい。木の中身を絞り込む・並べ替えるような操作を足すなら、それは中身の操作なので木の近くに置く
    - 見出し（`#treeTitle`）はスイッチに連動して「石の分類マップ」「鉱物の分類マップ」に変える。ヘッダーで選んだ結果がページの見出しに出ないと、スイッチと中身のつながりが分からない
    - 下のほうで切り替えたら、新しい木の頭（紹介文）が上のバーのすぐ下に来るまで戻す。行き先は絶対位置で渡す
  - **石**は、左の「岩石」から右へ、でき方（火成岩・堆積岩・変成岩・鉱脈 → 深成岩・火山岩…）で枝分かれさせて、カタログの石を全部並べる
  - **鉱物**は、鉱物学の基本の分け方（何でできているか：ケイ酸塩・炭酸塩・酸化・硫化・元素）で分ける。ケイ酸塩鉱物は、ケイ素と酸素の骨組みのつながり方で、つながりの少ない順（ばらばら → 2つ組・輪 → 鎖 → 板 → 立体の網目）に並べる。石英は立体の網目としてケイ酸塩に入れる（酸化鉱物に分ける流儀もある）。雲母・輝石は、ほかの鉱物（黒雲母・白雲母、ひすい輝石）をまとめる枝で、鉱物のページへのリンクになる。組み方は `MINERAL_TREE`
  - 鉱物の札は名前だけ（鉱物のカタログは写真を持たない）。書き忘れた鉱物は根の下の「そのほか」に入る
  - 石と鉱物で違うところ（中身を並べる名前 `stones` / `minerals`、詳しいページ、書き忘れたものの置き場所）は `TREE_KINDS` にまとめ、組み立て（`buildTree()` / `toClusters()`）は共通
  - 石の枝の組み方は `rock-tree.js` の `TREE` に手で書いている。石は `id` だけ書き、名前・写真は `rocks.json` から引く（写真は `stonePhoto()`）。枝は用語ページ（`term`）か石のページ（結晶片岩）へのリンク
  - `TREE` に書き忘れた石は、`category` が同じ大枝の下に直接ぶら下げる（一覧から漏らさないため）。**石を足したら `TREE` にも正しい枝で書く**
  - 火山砕屑岩（凝灰岩・溶結凝灰岩・軽石）は堆積岩の枝に1つにまとめている。`rocks.json` の `category` は凝灰岩だけ堆積岩で残りは火成岩だが、でき方の枝として分けると同じ名前の枝が2か所にできるため。枝のひとことに「火成岩との境目」と書いてある
  - 石と枝が混ざる枝（広域変成岩の下の結晶片岩）は、並び順を崩さないよう、続いている石だけを1つの房にする（粘板岩・千枚岩／結晶片岩→3種／片麻岩・角閃岩）（`toClusters()`）
  - 石が1つだけの枝（変位変成岩）は、狭い画面で箱が折り返すと隣とぶつかるので、変成岩の枝の端に置いている
  - ページ上の説明は、地質の分類マップと同じ形（1文の紹介＋見方 `.tree-guide` の3行）。見本の石の札の写真は、`stonePhoto()` を通すため `fillGuideStone()` が入れる
- `litho-tree.html` / `litho-tree.js` — 地質の分類マップ。地図画面右上のメニューと、岩相ページの要点欄の「分類マップでこの地質を見る」から開く。地質図の岩相610件を、左の「地質」から右へ、区分（火成岩・堆積岩・付加体・変成岩・その他）→ でき方で枝分かれさせる
  - **枝の先に並べるのは岩相そのものではなく「系統」（約120）**。610件のほとんどは同じ岩石の帯ちがい・時代ちがい（泥質片岩なら高P/T型のアルバイト黒雲母帯…で15件）で、区分と凡例記号の頭（`mscpe_hab` の `mscpe`）の組が系統にあたる。札には名前と件数（2件以上のとき）を出し、押すとその場で中身が開いて、そこから岩相ページへ行ける。1件だけの系統も同じく開く（直接リンクにすると、その地質の色を出す場所がなくなる）
  - 同じ記号の頭でも区分が違えば別の系統（`soss` は堆積岩にも付加体にもある）
  - 枝の組み方は `LITHO_TREE` に手で書く。`families` は `[記号の頭, 札の名前]` で、名前を省くと岩相名の最初の語を使う（変成岩は最初の語が岩石名なので省いてある）。書き忘れた系統は区分の大枝の下の「そのほか」に入る
  - 変成岩の名前は「泥質片岩・泥質グラノフェルス・泥質片麻岩…」と、もとの岩石を毎回くり返して長いので、2つめからは省く（`shortLabel()`。元の名前は札の `title`）
  - 開いた中身は、頭の語が2種類以上あればそれで小見出しを立てる（変成岩なら 高P/T型・中P/T型・低P/T型 ごとに帯を並べる）
  - **地質図の色は、岩相1つに1色ではなく「岩相 × 時代」ごと**（全2416色、1つの岩相に最大26色。付加体の砂岩は時代ちがいで黄色の濃淡22色）。だから系統の札には色を出さない。以前は系統で最初に出てくる1色を出していたが、たまたまの色で意味がなかった
  - 色は、開いた中身の岩相ごとに、`lithology-detail.json` の `ages`（時代と色、古い順）を全部小さな丸で並べる。丸に触れると時代名が出る（`title`）。地図画面で地質図を重ねたときの答え合わせ用
  - 開いた中身は岩相1件を1行にし、名前を左、色を右に**1列で**並べる（折り返さない）。幅が足りなければ丸が細い縦棒まで縮む。そのため丸の幅は `flex-basis` ではなく `width` で持たせ（basis だけだと入れ物の幅が中身なしで計算され、広い画面でも2pxに縮む）、一覧の列は `minmax(0, 1fr)` にしている（auto だと行の最小幅で列が広がり、狭い画面ではみ出す）
  - ページ上の説明は、1文の紹介と「見方」（`.tree-guide`）の3行。見方の見本（札・色の丸）は木の中と同じ部品で描いて、同じものを探せるようにしている
  - 見方の2行目に「全部開く／全部閉じる」（`#openAllButton`）。全部開くと610件が並び、PCで約23000pxになる。読み込み前は押しても何も起きないので `hidden` にしておき、`litho-tree.js` が出す
    - 見た目は、うすく色の付いた丸いボタンに上下に開く矢印（`.open-all-up` / `.open-all-down`）。触れると矢印が上下に広がる。全部開いているあいだは塗りを濃くし、矢印がそれぞれ半回転して内向き（閉じる）になる。全体の `button` の指定（高さ42px・塗りつぶし）は打ち消している
  - `?id=岩相の記号` で開くと、その系統を開いて、その岩相の位置までスクロールする
  - 枝が5段と深く札も多いので、広い画面でも枝の箱を細くして（150px）房に幅を回している
  - 全体を開かずに、PC（1280×800）で約1700px。スマホでは札が1列に近くなり、375px幅で約3600pxと長い
  - **開閉する札は `button`。全体の `button` の指定（`min-height: 42px`・押すと沈む）を `.litho-chip` で打ち消している**。打ち消さないと札が全部42pxの高さになり、木が約1.3倍に伸びた
- `tree-layout.js` — 分類マップ（石・鉱物・地質）の木を描く共通の部品（`mountTree()`）。同じ枠に別の木を描き直してもよく、そのときは幅の変化などで組み直すのは最後に渡した木になる。左の根から右へ枝分かれし、枝の先の中身は横並びの房（`.cluster`）にまとめて折り返す。房の中身の `<li>` はページごとに作って渡す
  - **石を1行に1つ並べる系統樹の形はやめた**。木が縦に約1600pxまで伸びて、「岩石 → 火成岩 → 深成岩」の骨組みがスクロールしないと見渡せなかった。房にすると段の数が石の数（33）から房の数（約12）に減り、石の分類マップはPCで約725pxでほぼ1画面に収まる
  - 横幅は枠いっぱい（横スクロールはしない）。房は折り返しで高さが変わるので、いったん上端に並べて房と枝の箱の高さを測ってから縦の位置を決める（測り終えるまで `.tree-list` は `visibility: hidden`）。線は後ろの SVG に描く。枠の幅が変わったら `ResizeObserver` で組み直す
  - いちばん深い房にも `minCluster` の幅が残るよう、足りなければ枝の箱を `minNode` まで細くする。広い画面では1段の高さが枝の箱で決まるので、幅に余裕があれば190pxにして枝のひとことを1行に収める
  - 狭い画面（640px以下）では、根と大枝を子の範囲いっぱいに伸ばした縦書きの帯にする。帯の文字は `position: sticky` で画面に残す（そのため木の入れ物を `overflow: auto` にしない。すると sticky が効かなくなる）。枝のひとことは出さない
  - 枝や房の中身に触れると、そこから根までの道筋だけを濃くする（`bindTreeHighlight()`。房の中身の要素には `data-cluster` を付ける）
  - 房の `li` の幅の上限（`max-width: 100%`）は札ではなく `li` に持たせる。札に付けると `li` の幅と循環して最小の幅で計算され、短い名前まで2行に折れる
- `menu.js` — 地図画面右上のメニューの開閉。押すと右から引き出し（ドロワー）が出る。ハンバーガーは開くと×に変わり、背景を押す・Esc・もう一度押すで閉じる。閉じているあいだは `inert` にしてタブ移動から外す
  - 引き出しの中は項目だけ。「メニュー」の見出しや区切り線、閉じる×は置かない
  - **ボタンはヘッダーの外に出して `position: fixed`**（z-index 2200）。ヘッダーの中に置くとヘッダーが重なり順の入れ物になり、ボタンだけを引き出し（2000）より前に出せない。前に出せないと、開いたときにボタンが引き出しの下に隠れてしまう
  - ボタンは開くと3本線が×に変わる。位置は変わらないので、同じ場所で開閉できる
- `link-preview.js` — 詳細ページ（石・鉱物・岩相・用語）の、石・鉱物・用語へのリンクにマウスを乗せると説明カードを出す。地図画面は `<body data-link-preview="off">` でカードだけ止めている（検索結果でカードを出す必要はない。ただし `catalogLink()` は地図カードの鉱物名で使うので、ファイルごと外すことはできない）。押すとそのまま詳細ページへ。中身は `data/bundle.js` から作るので、どのページも `bundle.js` を読んでおくこと。触るだけの端末では出さない
  - カードの置き場所は 下 → 上 → 右 → 左 の順に、画面に全部収まる最初の場所。縦長（写真が上）でどこにも入らなければ、写真を左に置いた背の低い横長（`is-wide`）で試し直す。収まるかは `max-height` で切られた高さではなく `scrollHeight`（中身が全部見える高さ）で判定する
  - **石の画像は `stoneImageSrc()`（同じファイル）1つから作る**。石の詳細ページの写真、「間違えやすい石」や用語ページの一覧の写真（`catalogRow()`）、岩相ページの写真タイル（`stoneTile()`）、説明カードの写真がすべてこれを通るので、`rocks.json` の `images[]` を差し替えればどこも同時に変わる。別の場所で画像のパスや模様を組み立て直さない（地図カードの `rockImageSrc()` は旧来の別実装）
  - 各ページは fetch で読んだ rocks / minerals / terms を `registerCatalog()` で渡す。リンクやカード、間違えやすい石の写真はそれを優先し、無いときだけ `data/bundle.js` を使う。`rocks.json` を直してビルドし忘れても、古い写真が一部にだけ残らないようにするため
  - **上のバーの戻るボタン（`#backButton`）の処理もここ（`bindBackButton()`）**。詳細ページ4つと用語集が共通で使う。読み込み時に自分で結びつけるので、ページ側から呼ばない（呼ぶと二重に結びついて、`history.back()` が2つ戻る）
    - 詳細ページは地図から `target="_blank"` で開くので、そのタブには履歴がない。ここで `index.html` を開くと、そのタブにもう1枚地図が出てしまい、選んだ地点も結果も残っているもとの地図が裏のタブに置き去りになる。だから `window.opener` があれば `window.close()` でタブごと閉じる
    - **そのために、自分のページへのリンクには `rel="opener"` を明示してある**（同じオリジンの自分のページなので問題ない）。**`rel` を書かないだけでは足りない。いまのブラウザは `target="_blank"` に既定で noopener を付けるため（Chrome 88 以降）、`window.opener` は null のままになる。** 外部サイトへのリンク（地図の出典・写真のライセンス）には `noopener` を付けたまま
    - **`history.length` で「新しいタブか」を判定しない。** 空のタブから開くと、どこにも移動していなくても2になる
    - ブラウザが閉じるのを拒むことがあるので、300ms後に `index.html` へ行く保険を入れてある
  - 名前をリンクにするのは `catalogLink()`（同じファイル）。石・鉱物・用語のカタログに同じ名前があればリンク、なければ文字のまま。「輝石・角閃石」のような並びは1語ずつ見る。石の「主な造岩鉱物」「間違えやすい石」「関連用語」、鉱物の「間違えやすい鉱物」、地図カードの「主な鉱物」が使っている
  - `CATALOG_ALIASES` には、カタログの項目の仲間だと確かな言葉だけを入れる（斜長石→長石、陽起石→角閃石 など）。似ているだけの別物を入れると違うページへ案内してしまう
- `classification.js` — 岩石の「名前の決まり方」を物差し（横棒）で描く。石と岩相の詳細ページが共通で使う。物差しの定義（`CLASSIFICATION_SCALES`）と出典（`CLASSIFICATION_SOURCES`）はここにある。ヘルプのポップアップ（`<dialog>`）もここ。図の上には「玄武岩は、シリカ（SiO₂）の量が45〜52%の岩石です」という要約の文と、その区間の上に石の名札を出す。物差しだけだと、見出しと横棒の関係がひと目で分からないため
- `styles.css` — 全体のスタイル
  - 日本語は Noto Sans JP（Google Fonts）、欧文は端末のUIフォント。`font-family` に両方並べているので、日本語の字だけが Noto に落ちる。読み込めなくても端末のゴシック体になるだけで崩れない（`file://` でも同じ）
  - 詳細ページは1行40字前後に収める。日本語は1行が長いと次の行の頭を探しにくい。石・岩相・用語のページは広い画面で2段組み（`.detail-layout`：左に本文700px、右に写真や要点・目次340px。目次は sticky）、狭い画面では 名前 → 要点 → 本文 の1列。鉱物・用語集のページは `.stone-detail.is-narrow`（760px）の1段
  - 上のバーの戻るボタンと、地図画面のメニューボタンは、枠線も文字も持たないアイコンだけの丸ボタン（名前は `aria-label` と `title`）。バーの高さは60pxで、sticky な要素（用語集の五十音の帯など）の `top` もこれに合わせる
  - 詳細ページ（`html.stone-root`）でスクロールするのは `html` だけ。`body` まで `overflow: auto` にすると、上のバーや用語集の索引の `position: sticky` が効かなくなる
- `data/lithology-map.json` — 岩相(symbol接尾辞) → 石 の対応表。**生成物なので手で編集しない**
- `data/legend-index.json` — 凡例の色 → [symbol接尾辞, 時代コード]。**生成物**。地質図タイルの画素から地質を逆引きするために使う
- `data/rocks.json` — 石のカタログ。地図画面のカードと `stone.html` の両方がこれ1つを参照する
  - `identification` / `confusedWith` / `fieldTests` が現場での同定用。写真がなくても石を判別できるようにするための情報で、詳細ページの最上段に置いている
  - `classification` は名前の決まり方。`scales` にどの物差しのどの区間がこの石かを持ち、`note` に説明を書く。存在しない物差しや区間を指すと `checkClassification()` がビルドを止める
  - **物差しの数値は国際分類の境界そのものだけ**（深成岩＝IUGS の QAPF 等、火山岩＝TAS の SiO₂、堆積岩＝Wentworth の粒径、火山砕屑岩＝Schmid）。岩石ごとの「よくある割合」は出典をそろえられないので出さない。以前そうしたグラフを作ったが、根拠がなかったので全部やめた
  - 数値を足すときは、必ず出典を確かめて `CLASSIFICATION_SOURCES` に入れる。TAS の区間は pyrolite（Le Maitre 2002 の座標）で確認済み。デイサイトと流紋岩の境目は (69, 8)–(77.3, 0) の斜めの線で、SiO₂ だけでは決まらないので斜線の区間にしている
  - 変成岩・石灰岩・チャート・黒曜石・軽石などは、見た目のつくりや中身で決まる名前なので `scales` を空にして `note` だけ書く。変成岩の分類（SCMR）は数値を確かめられていないので、数字は入れていない
  - `confusedWith[].name` にはカタログ外のもの（コンクリート、スラグ、方解石など）も入る。取り違えの実態に合わせるため、`rocks.json` の `name` に限定しない
- `data/minerals.json` — 鉱物のカタログ。母岩と変成帯から候補を出す
  - `identification` / `confusedWith` / `fieldTests` は石と同じ意味で、`mineral.html` の最上段に置いている
  - `hosts` の石 `id` は `rocks.json` に存在させる（詳細ページで母岩へのリンクになる）
  - 石のページに名前だけ出ていた鉱物（赤鉄鉱・滑石・ドロマイト・クロムスピネル・雲母・黄銅鉱・金）は `hosts` と `zones` を空にして入れてある。得点が0になるので地図の「見どころの鉱物」には出ず、用語集とリンク先のページとしてだけ働く
  - 整形は features / hosts / zones を1行、fieldTests と confusedWith を複数行にそろえる（`JSON.stringify(..., 2)` でそのまま書き出すと全部が展開されて差分が大きくなる）
  - `notIn` は、岩相名にその言葉があるとき、その地質ではできない鉱物として除くためのもの。母岩からの判定と変成帯からの判定の両方に効く。菫青石・珪線石は泥質の岩石が低圧側で変成したときにしかできないので、高P/T型や苦鉄質・石灰質の地質では出さない、など
  - `zones` のうち「○○相」（角閃岩相など）は、もとが玄武岩質の岩石を基準に決めた名前。泥質の地質に当てはめると外れるので、重みを低めにするか `notIn` で除く
- `data/lithology-detail.json` — 岩相 → 英名と、地質図に出てくる時代・色（古い順）。**生成物**。岩相の詳細ページだけが使う
- `data/terms.json` — 用語のカタログ。石・鉱物のページに出てくる言葉（シリカなど）を説明する
  - `reading` はひらがなの読み（用語集の並び順と検索に使う）。石・鉱物にも同じ `reading` があり、無い・ひらがなでないと `checkReadings()` がビルドを止める。`category` は「地質」か「用語」（用語集の絞り込み）。`aliases` は別の呼び方（「三波川帯」など）。`catalogLink()` は別名でもリンクにする
  - `sections[].items[].link` は `{ type: "stone" | "mineral" | "term", id }`。リンク先が実在しないと `checkTermLinks()` がビルドを止める
  - `shortDescription` は、用語リンクにマウスを乗せたときの説明カード（`link-preview.js`）にも使う。1〜2文に収める
  - 数値を書くときは `sources` に出典を入れる
- `data/bundle.js` — rocks / lithology-map / legend-index / minerals / terms を1つにまとめたもの。**生成物**。`file://` で開くと `fetch` が使えないので、そのときだけこちらから読む
- `data/lithology-detail-bundle.js` — `lithology-detail.json` の `file://` 用。**生成物**。`bundle.js` と同じくらい大きく、`bundle.js` は地図画面が毎回読むので分けてある
- `tools/build-lithology-map.mjs` — 生成物（lithology-map / legend-index / lithology-detail / 2つの bundle / PHOTO-CREDITS.md）を作るスクリプト
- `api/geology.php` — CORS 対策のフォールバック用プロキシ
- `local-server.cjs` — Node だけで動く確認用サーバー

## 起動

```bash
node local-server.cjs
```

`http://localhost:8000` を開く。PHP プロキシの挙動を確認したいときだけ `php -S localhost:8000` を使う。

`index.html` をダブルクリックして `file://` で開いても動く。ただし `fetch` が使えないため石データは `data/bundle.js` から読み、面積比の測定（canvas）も無効になる。**`data/*.json` を編集したら `node tools/build-lithology-map.mjs` を実行しないと `file://` 側に反映されない。**

## 外部 API

```text
凡例(矩形): https://gbank.gsj.jp/seamless/v2/api/1.3/legend.json?box=南,西,北,東
凡例(点)  : https://gbank.gsj.jp/seamless/v2/api/1.3/legend.json?point=緯度,経度
凡例(全件): https://gbank.gsj.jp/seamless/v2/api/1.3/legend.json
地質タイル: https://gbank.gsj.jp/seamless/v2/api/1.3/tiles/{z}/{y}/{x}.png
地理院タイル: https://cyberjapandata.gsi.go.jp/xyz/std/{z}/{x}/{y}.png
住所逆引き: Nominatim (OpenStreetMap)
```

凡例レスポンスの主なフィールドは `symbol` `title` `group_ja` `lithology_ja` `formationAge_ja` `value`(色)。

`group_ja` は 5 種類しかない: 付加体 / 堆積岩 / 火成岩 / 変成岩 / その他。
岩相 (`lithology_ja`) は 610 種類あり、`symbol` から時代プレフィックスを除いた接尾辞（`K22_pbg_a` → `pbg_a`）と 1 対 1 で対応する。

## 地点の選び方

- **検索・現在地**は「そこを調べたい」という意思が明確なので、`selectAndInspect()` でそのまま地質取得まで走る
- **地図クリック**は誤操作もあるため、ポップアップの「ここを調べる！」を押すまで待つ。このときパネルの案内文を `text.pressInspect` に差し替える（「まず場所を選んでください」のままだと、選んだのに何も起きていないように見える）

## 石の判定について

判定は `script.js` の `estimateRocks()` にまとまっている。

1. クリック地点は `?point=` で引く（必ず 1 件に決まる）。周辺は `?box=` で ±0.01 度の範囲を引く
2. 凡例の `symbol` 接尾辞で `lithology-map.json` を辞書引きする。部分文字列マッチはしない
   - 対応表には、地質図に描かれない**随伴物**も入れてある（`addCompanions()`）。20万分の1では石英脈（幅数cm〜数m）・接触変成帯（幅数百m）・軽石層は表現できないが、石拾いでは普通に手に取る。脈石英は596岩相すべてに入る
   - これを入れる前は岩相の9割が石を1〜2種しか返しておらず、候補が少ない主因だった（平均1.63種 → 2.81種）
3. 地点の岩相の石を weight × 1.0、周辺の石を weight × 0.4 で採点する。周辺のぶんには次の3つを掛ける
   - **面積比**（`areaBoost`）— 地質図タイルをcanvasに描いて画素の色を数え、その地質が範囲の何%を占めるかを測る。凡例APIは「範囲内にあるか」しか返さないので、端をかすめただけの岩体と広大な岩体が同格になってしまう。凡例の色は2416件すべて一意なので画素から逆引きできる
   - **時代**（`ageDurability`）— 堆積岩・付加体のみ。古くて締まった砂岩ほど礫として残る
   - **標高**（運搬モードのみ）— 地理院の標高タイルを同じ範囲で読み、クリック地点より高い場所を最大2.2倍重く数える。石は上流から来るため
4. 地点が未固結堆積物や埋立地（`kind !== "bedrock"`）のときは**運搬モード**に切り替える。足もとの地質からは石を決めず、周辺の岩盤を採点し、さらに `DURABILITY`（川で運ばれるあいだの残りやすさ）を掛ける。段丘堆積物・河川堆積物・扇状地などがこれにあたり、石拾いではむしろ本命の場所になる
5. 運搬モードでは `sampleCatchment()` で**上流域**を割り出し、その中の地質を運搬元として採点する。標高タイルから「地点から標高が下がらない経路でたどり着ける範囲」を塗りつぶす（尾根を越えるには一度下る必要があるのでそこで止まる）。多摩川の世田谷で約1227km²（実際の流域は約1240km²）。上流域が取れたら周辺の段階は近い2つだけに絞る。四方に広げると下流や別の谷まで混ざるため
   - 正攻法は窪地を埋めてからD8法で逆追跡だが、`sink filling` なしのD8は実測でまったく機能しなかった（どの地点でも上流域が数画素）。この簡易版のほうが実用になる
   - 山間の狭い谷（長瀞など）では上流域が小さく出る。`CATCHMENT_MIN_CELLS` を下回ったら範囲方式に落ちる
6. 周辺は `NEARBY_STEPS`（±1 → 4 → 11 → 28 → 55km）を**すべて重ねる**。川の石は何十kmも上流から来るので、近くに岩盤があってもそこで打ち切ると実際に落ちている石のごく一部しか出てこない。遠い段階ほど係数を下げる（`CARRIED_FACTORS` / `NEARBY_FACTORS`）ので、遠くの石が「よく見つかる」になることはない
   - 足もとが岩盤なら3段階（±11kmまで）、運搬モードなら4段階（±28kmまで）
   - ±55km は API の応答に10秒以上かかるので、候補が `WIDEN_WHEN_FEWER_THAN` 未満のときだけ使う。しかも**先に結果を描画してから後追いで足す**（`inspectGeology` の `draw()`）。大河川の下流はこれがないと候補が3種しか出ない
7. 得点 0.75 以上を「よく見つかる」、0.4 以上を「見つかるかも」、それ未満を「珍しい」に振り分ける。ただし岩盤がどこも遠い平野では全体の点が低くとどまり全部「珍しい」になってしまうので、その場所の最有力候補の `LEVEL_RELATIVE` 倍以上なら「見つかるかも」に上げる
8. `ROCK_FLOOR` 未満は捨てる。遠くにわずかに分布するだけの石を並べても意味がない
9. 鉱物（`estimateMinerals`）は2通りで拾う。どちらも地質ごとに `notIn` を照らすため、石の得点は `byLithology`（どの地質から何点もらったか）も持っている。母岩の得点から導くものと、変成帯の名前から導くもの。`lithology_ja` の「ざくろ石帯」「カリ長石菫青石帯」「ローソン石青色片岩亜相」などは、そこに何が結晶しているかを直接示している

面積比・標高の測定は失敗しても推定は続く（`sampleNeighbourhood` が `null` を返すと重みなしになる）。canvasを使うのでローカルサーバー経由で開く必要がある。

地質情報がない場所（海など）では、APIが空レスポンスか全項目 `null` の凡例を返す。`normalizeLegendResponse()` が `symbol` の有無で弾いている。

## 意図的に扱っていないこと

- **ダム・護岸** — 一度は優先課題に挙げたが、影響を過大に見積もっていた。川原の礫は数千年かけて溜まったもので、日本のダムはせいぜい70年。下流でも上流由来の石は堆積済みの礫として残るため、**石の種類の構成はほとんど変わらない**。ダムが効くのは河床低下や砂州の消失、つまり「拾える河原が残っているか」であって、このアプリが答える「どんな種類の石があるか」ではない
- **「見つかる度」の実地検証** — 閾値も係数もすべて机上で決めたもの。実際の採集記録と突き合わせていないので、順位はともかく「よく見つかる／見つかるかも」の境目に根拠はない

### 岩相 → 石 の対応で気をつけていること

- **超苦鉄質岩類（`pu_*`）の筆頭は蛇紋岩で、かんらん岩ではない。** 日本の超苦鉄質岩体は、地表に出てくる途中でほとんどが蛇紋岩に変質している。実際に拾えるのは蛇紋岩のほうで、かんらん岩は変質を免れた部分に限られる。アポイ岳（幌満かんらん岩体）が世界的に有名なのは、変質していないのが例外だから。白馬・八方尾根の超苦鉄質岩体も蛇紋岩マイロナイトとして知られる。以前は逆（かんらん岩1.0／蛇紋岩0.6）にしていて、八方尾根で「よく見つかる：かんらん岩」と出ていた
  - ただし「変成かんらん岩（`mrpr_*`）」「超苦鉄質片麻岩・グラノフェルス（`msgul_*` / `mghul_*`）」は岩相名そのものがかんらん岩を指す（接触変成で蛇紋石が脱水してかんらん石に戻る）ので、かんらん岩を筆頭のままにしている
- **岩屑なだれ堆積物・火山麓扇状地堆積物と、風成火山灰は中身が違う。** 前者は山体が崩れて転がってきた溶岩の塊（磐梯山の流れ山の中身は安山岩の塊、鳥海山の基質も安山岩岩塊と火山礫）なので安山岩が筆頭。後者は降り積もった火山灰なので軽石が筆頭。以前は3つをひとまとめにして軽石1.0にしていて、裏磐梯で「よく見つかる：軽石」と出ていた
- 岩相が1種類の岩体（超苦鉄質岩体など）の上に立つと「よく見つかる」が1つしか出ないが、これは不自然ではない。足もとの岩盤がその石そのものだから。河川や段丘のように、いろいろな石が混ざるのは運搬モードのほう
- **「その岩相の名前の石」と「そこで実際に拾える石」は別物**という目で対応表を見る。変質（かんらん岩→蛇紋岩）、産状（付加体の玄武岩→緑色岩）、堆積物の由来（岩屑なだれ→溶岩の塊）で、筆頭に置くべき石が変わる
- **年代によって「その形では残らない」石は、採点のときに落とす**（`script.js` の `AGE_FADE` / `ageFade()`）。`lithology-map.json` のキーは年代プレフィックスを外した接尾辞なので、対応表の側では年代が分からない。だから `addRocks()` に年代（`ageCodeOf()` の 0=第四紀 … 4=古生代以前）を渡して、石ごとに掛けている
  - 軽石 … 埋もれて固結すると凝灰岩になる。第四紀 1.0 → 新第三紀 0.3 → それより古いと0
  - 黒曜石 … 火山ガラスは時間とともに結晶化して失われる。日本の産地（白滝・霧ヶ峰・神津島・隠岐・姫島・腰岳など）はいずれも第四紀の火山。第四紀 1.0 → 新第三紀 0.2 → それより古いと0
  - 脈石英 … 熱水が回って脈ができるには時間と埋没が要る。第四紀 0.25 → 新第三紀 0.85 → それより古いと1.0。これで富士山の溶岩からは脈石英が消え、秩父の付加体では0.55のまま残る
  - 効きの確認: 富士山（完新世の玄武岩）＝脈石英が消える／秩父（ジュラ紀付加体）＝脈石英0.55のまま／神津島（完新世のデイサイト・流紋岩）＝黒曜石0.25が残る／秋田の中新世の海成層＝黒曜石なし・脈石英0.26

対応表を作り直したいときは `node tools/build-lithology-map.mjs` を実行する。ルールはこのスクリプトの中にあり、`data/lithology-map.json` は生成物なので直接編集しない。

## 写真の扱い

現在は実物の写真を使っておらず、`images[].palette` からSVGの模様を生成して代用している。

写真を入れるときは `data/rocks.json` の `images[]` に次をそろえる。**`src` を入れたのに出典が欠けていると、`build-lithology-map.mjs` がビルドを中止する**（`checkPhotoCredits()`）。表記のない写真が公開されないようにするための仕掛けなので、この検査を緩めない。

```json
{
  "label": "ゴマ塩模様",
  "src": "photos/granite-1.jpg",
  "credit": "撮影者名または提供機関名",
  "license": "CC BY 4.0",
  "licenseUrl": "https://creativecommons.org/licenses/by/4.0/",
  "sourceUrl": "https://…（ファイルそのもののページ）",
  "modified": true,
  "palette": ["#d8d3ca", "#5d5952", "#f2efe8"],
  "seed": 7
}
```

- `modified` はサイズ変更や切り抜きをしたときに `true`。CC BY / CC BY-SA は改変の明示が要る
- `palette` は写真が読めなかったときのために残しておく
- 出典は写真を出すすべての場所に表示する（詳細ページは `photoCredit()`、地図カードは `rockPhotoCredit()`）
- `PHOTO-CREDITS.md` は `rocks.json` から自動生成する。手で書くと必ずずれるので編集しない
- **CC BY-SA** は改変物に同ライセンスの継承義務がある。縮小しただけでも改変にあたるので、採用するなら承知のうえで

## 書き方の約束

- 日本語文字列はそのまま書く。サーバーもHTMLもUTF-8を明示している（`\uXXXX` エスケープは旧コードの名残）
- HTML を組み立てる場所では必ず `escapeHtml()` を通す
- `lithology-map.json` と `minerals.json` が参照する石 `id` は、すべて `rocks.json` に存在させる（欠けると詳細ページが「見つかりませんでした」になる）。生成スクリプトを直したら整合を確認する
